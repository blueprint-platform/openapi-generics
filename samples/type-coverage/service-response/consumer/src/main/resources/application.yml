server:
  port: ${APP_PORT:8075}
  servlet:
    context-path: /type-coverage/service-response-consumer
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never
    include-exception: false

spring:
  application:
    name: service-response-consumer
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    io.github.blueprintplatform: INFO

type-coverage:
  api:
    base-url: ${TYPE_COVERAGE_API_BASE_URL:http://localhost:8074/type-coverage/service-response}